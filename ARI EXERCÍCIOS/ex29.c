#include <stdio.h>
#define MAX 100

int main() {
    char s1[MAX], s2[MAX];
    printf("Digite a primeira string"); 
    gets(s1);
    printf("\nDigite a segunda string");
    gets(s2);

    return 0;
}

