#include <stdio.h>
#define MAX 100
//encontre todas as palavras palindromos em uma frase
int main() {
    
    char palindormo[MAX], frase[MAX];
    printf("\nInforme a frase"); 
    fgets(frase, MAX, stdin);
    for (int i = 0; i < strlen(frase); i++)
    {
        
    }
    

    return 0;
}
